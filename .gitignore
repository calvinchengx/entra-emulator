# Runtime state
data/
*.log

# Node
node_modules/

# Python
.venv/
__pycache__/

# Local tooling
.claude/

# Note: portal/dist IS committed — go:embed needs it for `go build` to be
# self-sufficient. Rebuild with `cd portal && npm run build`.

# Xcode/Flutter build output that lands outside the app dir
e2e/build/
